version: 1.0
uown: eefa6898-cb13-4fd0-9a6f-ee3ff558888d #upinot
command: |
  export NODE_ENV=development; git checkout $BUILDKITE_BRANCH; if [ -f ".buildkite/build.sh" ]; then .buildkite/build.sh; fi;
timeout_in_minutes: 300
